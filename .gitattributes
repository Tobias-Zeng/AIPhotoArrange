# Enforce Windows CRLF line endings for batch scripts so cmd.exe parses
# multi-line ( ... ) blocks correctly. Bare LF breaks .bat parsing.
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
