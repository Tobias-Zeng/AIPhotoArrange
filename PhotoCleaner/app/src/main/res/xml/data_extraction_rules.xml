<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ 备份/迁移规则。配合 android:allowBackup="false"：
  云备份与设备迁移都不导出应用数据，防止 auth_token / 加密存储被 adb backup
  或云同步带出设备。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
